image: registry.gdut.edu.cn/docker/library/ubuntu:22.04

stages:
  - check
  - package-build
  - image-build
  - release
  - signing
  # - deploy

cache:
  paths:
    - cache

sonarqube-check:
  stage: check
  image: registry.gdut.edu.cn/docker/library/maven:3-eclipse-temurin-17
  tags:
    - maven
  before_script:
    - echo "=========================== CHANGE MAVEN MIRROR ============================"
    - mkdir -p $HOME/.m2 || true
    - echo '<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 https://maven.apache.org/xsd/settings-1.2.0.xsd">
      <localRepository>/cache/.m2</localRepository>
      <mirrors>
      <mirror>
      <id>nexus-aliyun</id>
      <mirrorOf>*</mirrorOf>
      <name>Nexus aliyun</name>
      <url>https://mirrors.gdut.edu.cn/nexus/repository/maven-public/</url>
      </mirror>
      </mirrors>
      </settings>' > $HOME/.m2/settings.xml || true
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"  # Defines the location of the analysis task cache
    GIT_DEPTH: "0"  # Tells git to fetch all the branches of the project, required by the analysis task
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  script:
    - mvn verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -DskipTests -Dmaven.repo.local=cache -f server/pom.xml
  allow_failure: true
  only:
    - dev

package-build:dev:
  image: registry.gdut.edu.cn/docker/library/ubuntu:22.04
  stage: package-build
  tags:
    - dev
  before_script:
    - echo "============================ CHANGE APT MIRROR ============================="
    - echo 'deb http://mirrors.gdut.edu.cn/ubuntu/ jammy main restricted universe multiverse
      deb-src http://mirrors.gdut.edu.cn/ubuntu/ jammy main restricted universe multiverse
      deb http://mirrors.gdut.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse
      deb-src http://mirrors.gdut.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse
      deb http://mirrors.gdut.edu.cn/ubuntu/ jammy-backports main restricted universe multiverse
      deb-src http://mirrors.gdut.edu.cn/ubuntu/ jammy-backports main restricted universe multiverse
      deb http://mirrors.gdut.edu.cn/ubuntu/ jammy-security main restricted universe multiverse
      deb-src http://mirrors.gdut.edu.cn/ubuntu/ jammy-security main restricted universe multiverse' > /etc/apt/sources.list
    - echo "======================== INSTALL REQUIRED TOOLS ============================"
    - apt update -y
    - apt install -y --no-install-recommends ca-certificates curl wget git gnupg2 software-properties-common
    - apt install -y --no-install-recommends openjdk-17-jdk
    - apt install -y --no-install-recommends maven
    - echo "=========================== CHANGE MAVEN MIRROR ============================"
    - mkdir -p $HOME/.m2 || true
    - echo '<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 https://maven.apache.org/xsd/settings-1.2.0.xsd">
      <localRepository>/cache/.m2</localRepository>
      <mirrors>
      <mirror>
      <id>nexus-aliyun</id>
      <mirrorOf>*</mirrorOf>
      <name>Nexus aliyun</name>
      <url>https://mirrors.gdut.edu.cn/nexus/repository/maven-public/</url>
      </mirror>
      </mirrors>
      </settings>' > $HOME/.m2/settings.xml || true
    - echo "=========================== INSTALL NODE.JS ================================"
    - curl -fsSL https://deb.nodesource.com/setup_lts.x | bash -
    - apt-get install -y nodejs
    - echo "========================= CONFIGURE NPM REGISTRY ==========================="
    - npm config set registry https://mirrors.gdut.edu.cn/nexus/repository/npm
    - echo "============================== INSTALL PNPM ================================"
    - npm install -g pnpm
    - echo "========================= CONFIGURE PNPM REGISTRY =========================="
    - pnpm config set registry https://mirrors.gdut.edu.cn/nexus/repository/npm
  script:
    - echo "=========================== INSTALL DEPENDENCIES ============================"
    - pnpm install
    - echo "============================= BUILD FRONTEND ================================"
    - pnpm run build
    - mkdir -p server/src/main/resources/static
    - cp -r dist/* server/src/main/resources/static/
    - echo "========================== GENERATE VERSION TAGS ==========================="
    - export VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout -f server/pom.xml)
    # - export VERSION=v$VERSION
    - echo "Building version $VERSION"
    - echo "=========================== BUILD MAVEN PROJECT ============================"
    - mvn clean package -f server/pom.xml -Dmaven.test.sktip=true -DskipTests -Dmaven.repo.local=cache
    - echo "=========================== EXPORT VERSION TAGS ============================"
    - echo "VERSION=$VERSION" >> build.env
    - echo "============================= EXPORT JAR FILE =============================="
  artifacts:
    paths:
      - target/*.jar
    expire_in: 30 min
    reports:
      dotenv: build.env
  only:
    - dev

package-build:main:
  image: registry.gdut.edu.cn/docker/library/ubuntu:22.04
  stage: package-build
  tags:
    - dev
  before_script:
    - echo "============================ CHANGE APT MIRROR ============================="
    - echo 'deb http://mirrors.gdut.edu.cn/ubuntu/ jammy main restricted universe multiverse
      deb-src http://mirrors.gdut.edu.cn/ubuntu/ jammy main restricted universe multiverse
      deb http://mirrors.gdut.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse
      deb-src http://mirrors.gdut.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse
      deb http://mirrors.gdut.edu.cn/ubuntu/ jammy-backports main restricted universe multiverse
      deb-src http://mirrors.gdut.edu.cn/ubuntu/ jammy-backports main restricted universe multiverse
      deb http://mirrors.gdut.edu.cn/ubuntu/ jammy-security main restricted universe multiverse
      deb-src http://mirrors.gdut.edu.cn/ubuntu/ jammy-security main restricted universe multiverse' > /etc/apt/sources.list
    - echo "======================== INSTALL REQUIRED TOOLS ============================"
    - apt update -y
    - apt install -y --no-install-recommends ca-certificates curl wget git gnupg2 software-properties-common
    - apt install -y --no-install-recommends openjdk-17-jdk
    - apt install -y --no-install-recommends maven
    - echo "=========================== CHANGE MAVEN MIRROR ============================"
    - mkdir -p $HOME/.m2 || true
    - echo '<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 https://maven.apache.org/xsd/settings-1.2.0.xsd">
      <localRepository>/cache/.m2</localRepository>
      <mirrors>
      <mirror>
      <id>nexus-aliyun</id>
      <mirrorOf>*</mirrorOf>
      <name>Nexus aliyun</name>
      <url>https://mirrors.gdut.edu.cn/nexus/repository/maven-public/</url>
      </mirror>
      </mirrors>
      </settings>' > $HOME/.m2/settings.xml || true
    - echo "=========================== INSTALL NODE.JS ================================"
    - curl -fsSL https://deb.nodesource.com/setup_lts.x | bash -
    - apt-get install -y nodejs
    - echo "========================= CONFIGURE NPM REGISTRY ==========================="
    - npm config set registry https://mirrors.gdut.edu.cn/nexus/repository/npm
    - echo "============================== INSTALL PNPM ================================"
    - npm install -g pnpm
    - echo "========================= CONFIGURE PNPM REGISTRY =========================="
    - pnpm config set registry https://mirrors.gdut.edu.cn/nexus/repository/npm
  script:
    - echo "=========================== INSTALL DEPENDENCIES ============================"
    - pnpm install
    - echo "============================= BUILD FRONTEND ================================"
    - pnpm run build
    - mkdir -p server/src/main/resources/static
    - cp -r dist/* server/src/main/resources/static/
    - echo "========================== GENERATE VERSION TAGS ==========================="
    - export VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout -f server/pom.xml)
    - echo "Building version $VERSION"
    - echo "=========================== BUILD MAVEN PROJECT ============================"
    - mvn clean package -f server/pom.xml -Dmaven.test.sktip=true -DskipTests -Dmaven.repo.local=cache
    - echo "=========================== EXPORT VERSION TAGS ============================"
    - echo "VERSION=$VERSION" >> build.env
    - echo "========================== PUBLISH MAVEN PACKAGE ==========================="
    - curl --header "PRIVATE-TOKEN:${PACKAGE_PUBLISH_TOKEN}" --upload-file target/*.jar "https://git.gdutnic.com/api/v4/projects/${CI_PROJECT_ID}/packages/generic/CertVault/v${VERSION}/certvault-v${VERSION}.jar"
  artifacts:
    reports:
      dotenv: build.env
  only:
    - main

docker-build-and-push:
  image: registry.gdut.edu.cn/docker/library/docker:24-cli
  stage: image-build
  tags:
    - Docker
  services:
    - name: registry.gdut.edu.cn/docker/library/docker:24-cli
  before_script:
    - echo "======================== SETUP BUILDX ENVIRONMENT =========================="
    - docker info
    - sed -i 's#https\?://dl-cdn.alpinelinux.org/alpine#https://mirrors.cernet.edu.cn/alpine#g' /etc/apk/repositories
    - apk update
    - apk add --no-cache git
    # 创建并使用 buildx builder
    - docker buildx create --use --name multiarch-builder --driver docker-container --platform linux/amd64,linux/arm64,linux/arm/v5,linux/arm/v6,linux/arm/v7,linux/ppc64le,linux/riscv64,linux/s390x,linux/mips64le --bootstrap || docker buildx use multiarch-builder
    - docker buildx inspect --bootstrap
  script:
    - echo "================================= LOAD ENV ================================="
    - env
    - echo "VERSION=$VERSION"
    - echo "============================= LOGIN TO HARBOR =============================="
    - docker login -u ${USERNAME} -p ${PASSWORD} ${HARBORHOST}
    - echo "======================= BUILD AND PUSH DOCKER IMAGES ======================="
    - |
      docker buildx build \
      --platform linux/amd64,linux/arm64,linux/ppc64le,linux/riscv64,linux/s390x \
      --tag ${HARBORHOST}/certvault/certvault:${VERSION} \
      --tag ${HARBORHOST}/certvault/certvault:latest \
      --push \
      .
    - echo "============================ EXPORT IMAGE TAGS ============================="
    - echo "VERSION=$VERSION" >> build.env
  after_script:
    - echo "============================ CLEAN BUILD CACHE ============================="
    - docker buildx prune -a -f
  artifacts:
    reports:
      dotenv: build.env
    expire_in: 30 min
  only:
    - dev

helm-build:
  image: registry.gdut.edu.cn/docker/dtzar/helm-kubectl:3.8.0
  stage: image-build
  tags:
    - dev
  before_script:
    # 配置 Helm 和 Kubernetes
    - export KUBECONFIG=$CI_PROJECT_DIR/kubeconfig
    - echo "$KUBECONFIG_DATA" | base64 -d > $KUBECONFIG
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo update
  script:
    - echo "================================= LOAD ENV ================================="
    - env
    - echo "VERSION=$VERSION"
    - echo "============================ GO TO HELM DIRECTORY =========================="
    - cd server/helm/charts
    - echo "=============================== UPDATE VERSION ============================="
    - sed -i "s/@project.version@/${VERSION}/g" cert-vault/README.md
    - 'sed -i "s/version: 0.0.0/version: ${VERSION}/" cert-vault/Chart.yaml'
    - 'sed -i "s/appVersion: .*/appVersion: \"${VERSION}\"/" cert-vault/Chart.yaml'
    - echo "============================= LOGIN TO HARBOR =============================="
    - helm registry login ${HARBORHOST} --username ${USERNAME} --password ${PASSWORD}
    - echo "============================== PACKAGE CHART ==============================="
    - mv cert-vault certvault-helm
    - helm package certvault-helm --version ${VERSION}
    - echo "=============================== PUSH CHARTS ================================"
    - helm push certvault-helm-* oci://${HARBORHOST}/certvault
  artifacts:
    reports:
      dotenv: build.env
  only:
    - dev

release-new-version:
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  stage: release
  tags:
    - dev
  script:
    - echo "================================= LOAD ENV ================================="
    - env
    - echo "VERSION=$VERSION"
  release:
    tag_name: "${VERSION}"
    description: "This is a release of version ${VERSION}."
  allow_failure: true
  only:
    - main

image-sign:
  image: registry.gdut.edu.cn/docker/library/ubuntu:22.04
  stage: signing
  tags:
    - dev
  before_script:
    - echo "============================ CHANGE APT MIRROR ============================="
    - echo 'deb http://mirrors.gdut.edu.cn/ubuntu/ jammy main restricted universe multiverse
      deb-src http://mirrors.gdut.edu.cn/ubuntu/ jammy main restricted universe multiverse
      deb http://mirrors.gdut.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse
      deb-src http://mirrors.gdut.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse
      deb http://mirrors.gdut.edu.cn/ubuntu/ jammy-backports main restricted universe multiverse
      deb-src http://mirrors.gdut.edu.cn/ubuntu/ jammy-backports main restricted universe multiverse
      deb http://mirrors.gdut.edu.cn/ubuntu/ jammy-security main restricted universe multiverse
      deb-src http://mirrors.gdut.edu.cn/ubuntu/ jammy-security main restricted universe multiverse' > /etc/apt/sources.list
    - apt update -y
    - echo "======================== INSTALL REQUIRED TOOLS ============================"
    - apt install curl jq skopeo -y
  script:
    - echo "================================= LOAD ENV ================================="
    - env
    - echo "VERSION=$VERSION"
    - echo "============================== INSTALL COSIGN =============================="
    - curl -fL -o cosign_1.6.0_amd64.deb --insecure "https://cn-gz-1.minio.gdutnic.com/gdutnic/public/cosign_1.6.0_amd64.deb"
    - dpkg -i cosign_1.6.0_amd64.deb
    - mv /usr/local/bin/cosign-linux-amd64 /usr/local/bin/cosign
    - echo "=============================== KEY GENERATE ==============================="
    - export COSIGN_PASSWORD=$PASSWORD
    - cosign generate-key-pair
    - echo "============================= LOGIN TO HARBOR =============================="
    - cosign login -u ${USERNAME} -p ${PASSWORD} ${HARBORHOST}
    - echo "==================== EXTRACT ARCHITECTURE DIGESTS ========================="
    # 使用 skopeo 获取镜像 manifest 信息
    - |
      echo "Fetching manifest for ${HARBORHOST}/certvault/certvault:latest"
      skopeo inspect --raw docker://${HARBORHOST}/certvault/certvault:latest > manifest.json
      
      # 解析 manifest，提取每个架构的 digest
      cat manifest.json | jq -r '
        if .manifests then
          .manifests[] | "\(.platform.architecture):\(.digest)"
        else
          "single-arch:\(.config.digest // "unknown")"
        end
      ' > arch_digests.txt
      
      echo "Architecture digests:"
      cat arch_digests.txt
    - echo "===================== SIGN EACH ARCHITECTURE IMAGE ========================"
    # 签名 manifest list (整体镜像)
    - echo "Signing manifest list for latest tag..."
    - cosign sign --key cosign.key ${HARBORHOST}/certvault/certvault:latest
    # 签名每个架构的镜像
    - |
      echo "Signing individual architecture images..."
      while IFS=: read -r arch digest; do
        if [ "$arch" != "single-arch" ] && [ -n "$digest" ]; then
          echo "Signing $arch architecture with digest: $digest"
          cosign sign --key cosign.key ${HARBORHOST}/certvault/certvault@${digest}
        else
          echo "Skipping single-arch or empty digest"
        fi
      done < arch_digests.txt
    - echo "================================ CHART SIGN ================================"
    - cosign sign --key cosign.key $HARBORHOST/certvault/certvault-helm:${VERSION}
    - echo "========================== FIX FILE PERMISSIONS ============================"
    - chmod 644 cosign.key cosign.pub
    - ls -la cosign.*
    - echo "================================ EXPORT KEY ================================"
    - echo "The Password of the key is Harbor account's password, the key will reserved for 30 minutes."
    - echo "VERSION=$VERSION" >> build.env
  artifacts:
    paths:
      - cosign.key
      - cosign.pub
      - arch_digests.txt
      - arch_digests_versioned.txt
      - manifest.json
      - manifest_versioned.json
    reports:
      dotenv: build.env
    expire_in: 30 min
  only:
    - dev

#helm-deploy:
#  stage: deploy
#  image: registry.gdut.edu.cn/docker/dtzar/helm-kubectl:3.8.0
#  tags:
#    - dev
#  before_script:
#    # 配置 Helm 和 Kubernetes
#    - export KUBECONFIG=$CI_PROJECT_DIR/kubeconfig
#    - echo "$KUBECONFIG_DATA" | base64 -d > $KUBECONFIG
#    - helm repo add stable https://charts.helm.sh/stable
#    - helm repo update
#  script:
#    - echo "================================= LOAD ENV ================================="
#    - env
#    - echo "VERSION=$VERSION"
#    - echo "============================= LOGIN TO HARBOR =============================="
#    - helm registry login -u ${USERNAME} -p ${PASSWORD} ${HARBORHOST}
#    - echo "============================= PULL NEW IMAGE ==============================="
#    - helm pull oci://${HARBORHOST}/certvault/certvault-helm --version ${VERSION} --untar
#    - echo "======================== LOAD HELM CONFIGURATION ==========================="
#    - rm ./certvault-helm/values.yaml
#    - echo ${VALUES_BASE64} | base64 -d > ./certvault-helm/values.yaml
#    - echo "=========================== DEPLOY NEW VERSION ============================="
#    - helm upgrade --install certvault4 ./certvault-helm --namespace certvault --set image.tag=${VERSION}
#  only:
#    - dev